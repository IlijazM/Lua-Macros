package task;

import macro.Macro;
import macro.MacroEvents;

class KeyPress extends Task {

	@Override
	public String name() {
		return "keypress";
	}

	@Override
	public void execute(String[] args, boolean fromKey) {
		if (args.length < 1) {
			return;
		}
		
		try {
			int keycode = Integer.parseInt(args[0]);
			
			MacroEvents.robot.keyPress(keycode);
		} catch (NumberFormatException e) {
			
		}
	}

}
